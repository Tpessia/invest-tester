import React from 'react';

// const AppLayout = lazy(() => import('@/modules/layout/components/AppLayout'));

const SuspenseFallback: React.FC = () => {
  return (
    <div></div>
  );
};

export default SuspenseFallback;
